# Mobile Application Binaries Directory

Place your test binaries in this directory:
- `test.apk` (Android Application Package)
- `test.app` (iOS Simulator Application Bundle) or `test.ipa` (iOS Real Device)

> The framework is configured by default to look for target binaries in this folder via `FrameworkConstants.APPS_PATH`.
